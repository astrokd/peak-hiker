---
title: Peal Hiker Home page
type: main
---

export const test = "test"

import { Link } from 'gatsby'
import { StaticImage } from "gatsby-plugin-image"
import Layout from '../components/layout'
import Image from '../components/image'
import ImageCarousel from '../components/imagecarousel'
import SEO from '../components/seo'
import { graphql } from "gatsby"

<SEO title="Home" keywords={['gatsby', 'application', 'react']} />

## Hi fellow Hikers!
## Welcome to Peak Hiker.

This is a blog about great pikes hikes in the Pacific North West and anywhere else great peaks can be found.

See <Link to="/peakhikes">Peak Hikes page</Link> for a link to current peak hike list.

<ImageCarousel img={props.data} />

export const query = graphql`
  {
    allFile(filter: {name: {in: ["mountsi","mailbox","teneriffe","mntdefiance","mtwashington","mcclellanbutte"]}}) {
      nodes {
        childImageSharp {
          gatsbyImageData
        }
        id
        name
      }
    }
  }
`
