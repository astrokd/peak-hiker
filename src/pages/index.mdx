---
title: Peal Hiker Home page
type: main
---

import { Link } from 'gatsby'
import { StaticImage } from "gatsby-plugin-image"
import Layout from '../components/layout'
import Image from '../components/image'
import ImageCarousel from '../components/imagecarousel'
import SEO from '../components/seo'
import { graphql } from "gatsby"

<SEO title="Home" keywords={['gatsby', 'application', 'react']} />

## Hi people!
## Welcome to Peak Hiker.

This is a blog about great pikes hikes in the Pacific North West and anywhere else great peaks can be found.

<Image img={props.data} alt="Mount Rainer" description="View of a mountain top" />

<ImageCarousel />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "mountrainer"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    }
  }
`
// query MyQuery {
//   allFile(filter: {name: {in: ["mountrainer","mountsi"]}}) {
//     nodes {
//       childImageSharp {
//         gatsbyImageData
//       }
//       id
//       name
//     }
//   }
// }

