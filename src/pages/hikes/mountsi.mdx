---
title: Mount Si
type: hike
---

import { Link } from 'gatsby'
import Layout from '../../components/layout'
import Image from '../../components/image'
import SEO from '../../components/seo'
import { graphql } from "gatsby"
import Map from '../../components/map';
import 'mapbox-gl/dist/mapbox-gl.css';
import ReactDOM from 'react-dom';
import HikeData from '../../components/hikedata'

<SEO title="Mount Si" />

## Mount Si

<Map title="Mount Si" Lat={47.495} Lng={-121.737} zoom={13} pitch={60} />

<HikeData 
  title="Mount Si via the Old Si trail"
  alltrailsurl="https://www.alltrails.com/trail/us/washington/mount-si-via-old-si-trail?u=i" 
  wtaurl="https://www.wta.org/go-hiking/hikes/mount-si" 
  distance="6.1" elevation="3556" peakheight="3900" />

<Image img={props.data} alt="Mount Si" description="View from top of Mnt Si" />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "mountsi"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
  }
`