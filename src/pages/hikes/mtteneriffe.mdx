---
title: Mount Teneriffe
type: hike
---

import { Link } from 'gatsby'
import Layout from '../../components/layout'
import Image from '../../components/image'
import SEO from '../../components/seo'
import { graphql } from "gatsby"
import Map from '../../components/map';
import 'mapbox-gl/dist/mapbox-gl.css';
import ReactDOM from 'react-dom';
import HikeData from '../../components/hikedata'

<SEO title="Mt Teneriffe" />

## Mt Teneriffe

<Map title="Mt Teneriffe" Lat={47.5021} Lng={-121.7071} zoom={13} pitch={50} bearing={70} />

<HikeData 
  title="Mount Teneriffe via the Kamikaze trail"
  alltrailsurl="https://www.alltrails.com/trail/us/washington/mount-teneriffe-via-kamikaze-trail?u=i" 
  wtaurl="https://www.wta.org/go-hiking/hikes/mount-teneriffe" 
  distance="7.4" elevation="3917" peakheight="4585" />

<Image img={props.data} alt="Mt Teneriffe" description="View from top of Teneriffe on a cloudy day" />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "teneriffe"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
  }
`