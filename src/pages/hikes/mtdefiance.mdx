---
title: Mount Defiance
type: hike
---

export const metadata = {
  title: "Mount Defiance",
  lat: 47.4220,
  lng: -121.5685,
  bearing: 35,
  pitch: 60,
  zoom: 13,
  distance: 11,
  elevation: 3584,
  peakheight: 5584,
};

import { Link } from 'gatsby'
import Layout from '../../components/layout'
import Image from '../../components/image'
import SEO from '../../components/seo'
import { graphql } from "gatsby"
import Map from '../../components/map';
import 'mapbox-gl/dist/mapbox-gl.css';
import ReactDOM from 'react-dom';
import HikeData from '../../components/hikedata'

<SEO title={metadata.title} />

<h2>{metadata.title}</h2>

<Map title={metadata.title} 
  Lat={metadata.lat} Lng={metadata.lng} 
  bearing={metadata.bearing} pitch={metadata.pitch} zoom={metadata.zoom} />

<HikeData 
  title="Mount Defiance via Ira Springs trail"
  alltrailsurl="https://www.alltrails.com/trail/us/washington/mount-defiance-and-mason-lake-trail?u=i" 
  wtaurl="https://www.wta.org/go-hiking/hikes/mount-defiance" 
  distance={metadata.distance} 
  elevation={metadata.elevation} 
  peakheight={metadata.peakheight} />

<Image img={props.data} alt={metadata.title} description="View from top of Defiance looking down on Mason lake" />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "mntdefiance"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
  }
`