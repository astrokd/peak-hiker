---
title: Mailbox Peak
type: hike
---

import { Link } from 'gatsby'
import Layout from '../../components/layout'
import Image from '../../components/image'
import SEO from '../../components/seo'
import { graphql } from "gatsby"
import Map from '../../components/map';
import 'mapbox-gl/dist/mapbox-gl.css';
import ReactDOM from 'react-dom';
import AllTrailsMap from '../../components/alltailsmap'
import ExternalLink from '../../components/externallink'

<SEO title="Mailbox" />

## Mailbox Peak

<Map title="Mailbox" mapstyle="mapbox://styles/kevek/ckyhun2330gf214pdia09g6qj" Lat={47.4642} Lng={-121.6585} bearing={70} pitch={50} zoom={13} staticMapImg={props.data} />

<ExternalLink url="https://www.wta.org/go-hiking/hikes/mailbox-peak-old-trail" text="WTA.org Mailbox Peak - Old Trail hike guide" />

<AllTrailsMap hike="mailbox-peak-old-trail" />

Mailbox peak is named so because of the mailbox that sits at the top of the peak.  There are 2 trails up the mountain, one is newer and has many switchbacks making it longer, and the other is the old mailbox peak trail which is considered one of the hardest in the area dues to its steepness.

<Image img={props.data} alt="Mailbox" description="View of the mailbox on Mailbox Peak" />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "mailbox"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
    mapImage: file(
      name: {eq: "placeholder"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
  }
`