---
title: Mailbox Peak
type: hike
---

export const metadata = {
  title: "Mailbox Peak",
  lat: 47.4642,
  lng: -121.6585,
  bearing: 70,
  pitch: 50,
  zoom: 13,
  distance: 4.8,
  elevation: 3959,
  peakheight: 4822,
};

import { Link } from 'gatsby'
import Layout from '../../components/layout'
import Image from '../../components/image'
import SEO from '../../components/seo'
import { graphql } from "gatsby"
import Map from '../../components/map';
import 'mapbox-gl/dist/mapbox-gl.css';
import ReactDOM from 'react-dom';
import HikeData from '../../components/hikedata'

<SEO title={metadata.title} />

<h2>{metadata.title}</h2>

<Map title={metadata.title} 
  Lat={metadata.lat} Lng={metadata.lng} 
  bearing={metadata.bearing} pitch={metadata.pitch} zoom={metadata.zoom} />
  
<HikeData 
  title="Mailbox Peak via the Old Mailbox Peak trail"
  alltrailsurl="https://www.alltrails.com/trail/us/washington/mailbox-peak-old-trail?u=i" 
  wtaurl="https://www.wta.org/go-hiking/hikes/mailbox-peak-old-trail" 
  distance={metadata.distance} 
  elevation={metadata.elevation} 
  peakheight={metadata.peakheight} />

<Image img={props.data} alt={metadata.title} description="View of the mailbox on Mailbox Peak" />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "mailbox"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
  }
`