---
title: Mailbox Peak
type: hike
---

import { Link } from 'gatsby'
import Layout from '../../components/layout'
import Image from '../../components/image'
import SEO from '../../components/seo'
import { graphql } from "gatsby"
import Map from '../../components/map';
import 'mapbox-gl/dist/mapbox-gl.css';
import ReactDOM from 'react-dom';
import HikeData from '../../components/hikedata'

<SEO title="Mailbox" />

## Mailbox Peak

<Map title="Mailbox" Lat={47.4642} Lng={-121.6585} bearing={70} pitch={50} zoom={13} />

<HikeData 
  title="Mailbox Peak via the Old Mailbox Peak trail"
  alltrailsurl="https://www.alltrails.com/trail/us/washington/mailbox-peak-old-trail?u=i" 
  wtaurl="https://www.wta.org/go-hiking/hikes/mailbox-peak-old-trail" 
  distance="4.8" elevation="3959" peakheight="4822" />

<Image img={props.data} alt="Mailbox" description="View of the mailbox on Mailbox Peak" />

export const query = graphql`
  {
    mountainImage: file(
      name: {eq: "mailbox"}
      ) {
      childImageSharp {
        gatsbyImageData
      }
      name
    },
  }
`