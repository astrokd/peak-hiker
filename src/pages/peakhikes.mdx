---
title: Peal Hiker hike page
type: hikes
---

import { Link } from 'gatsby'

import Layout from '../components/layout'
import Image from '../components/image'
import SEO from '../components/seo'
import { graphql } from "gatsby"
import HikeList from '../components/hikelist'

<SEO title="Home" keywords={['gatsby', 'application', 'react']} />

# Peak Hikes

What makes a peak hike a peak hike is the amount of physical exertion to climb to the top and the views when you get there.

<HikeList listTitle="Top I-90 Peak Hikes" hikePages={props.data.hikes.nodes} />

export const query = graphql`
  {
    hikes: allSitePage(filter: {path: {glob: "/hikes/*"}}) {
      nodes {
        id
        pageContext
        path
      }
    }
  },
`